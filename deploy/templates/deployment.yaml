kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ include "nextcloud.fpm.fullname" . }}
  namespace: {{ include "common.names.namespace" . }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: fpm
spec:
  {{- if not .Values.nextcloud.autoscaling.enabled }}
  replicas: {{ .Values.nextcloud.replicaCount }}
  {{- end }}
  strategy:
    {{- toYaml .Values.nextcloud.strategy | nindent 4 }}
  selector:
    matchLabels:
      {{- include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: fpm
  template:
    metadata:
      creationTimestamp: null
      labels:
        {{- include "common.labels.standard" . | nindent 8 }}
        app.kubernetes.io/component: fpm
    spec:
      restartPolicy: Always
      serviceAccountName: {{ include "nextcloud.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      initContainers:
        - name: {{ printf "%s-init" .Chart.Name }}
          image: {{ include "nextcloud.image" . }}
          imagePullPolicy: {{ include "nextcloud.imagePullPolicy" . }}
          volumeMounts:
            {{- include "nextcloud.volumeMounts" . | nindent 12 }}
          command:
            - php
            - /var/www/html/occ
            - status
      containers:
        - name: {{ .Chart.Name }}
          image: {{ include "nextcloud.image" . }}
          imagePullPolicy: {{ include "nextcloud.imagePullPolicy" . }}
          env:
            {{- include "nextcloud.env" . | nindent 12 }}
          ports:
            - name: fcgi
              containerPort: {{ .Values.nextcloud.containerPort }}
              protocol: TCP
          volumeMounts:
            {{- include "nextcloud.volumeMounts" . | nindent 12 }}
          {{- with .Values.nextcloud.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - |
                  export REQUEST_METHOD="GET"
                  export SCRIPT_FILENAME=/ping
                  export SCRIPT_NAME=/ping

                  RESPONSE=$(cgi-fcgi -bind -connect localhost:{{ .Values.nextcloud.containerPort }} 2>/dev/null | tail -n 1)

                  if [ "$RESPONSE" != "OK" ]; then
                    exit 1
                  fi
          readinessProbe:
            tcpSocket:
              port: {{ .Values.nextcloud.containerPort }}
            initialDelaySeconds: 5
            periodSeconds: 10
      serviceAccount: nextcloud
      volumes:
        {{- include "nextcloud.volumes" . | nindent 8 }}



